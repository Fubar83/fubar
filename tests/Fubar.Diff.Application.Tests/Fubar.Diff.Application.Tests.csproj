<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="xunit.runner.visualstudio" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Fubar.Diff.Application\Fubar.Diff.Application.csproj" />
    <!-- The real JsonAstParser, so the semantic-integration tests exercise the actual composition
         rather than a fake that agrees with whatever the code happens to do. Test projects may reach
         across layers; the production assemblies still may not, which LayeringTests enforces. -->
    <ProjectReference Include="..\..\src\Fubar.Diff.Infrastructure\Fubar.Diff.Infrastructure.csproj" />
  </ItemGroup>

</Project>
